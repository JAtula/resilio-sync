{
    "listening_port" : 55555,
    "storage_path" : "/mnt/sync/config",
    "vendor" : "docker",
    "display_new_version": false,

    "directory_root_policy" : "belowroot",
    "directory_root" : "$SYNC_DIR",

/*    "webui" :
    {
      	"listen" : "0.0.0.0:8888",
      	"allow_empty_password" : false,
        "dir_whitelist" : [ "$SYNC_DIR","/mnt/sync/folders", "/mnt/mounted_folders" ]
    } */
/* !!! if you set shared folders in config file WebUI will be DISABLED !!!
   shared directories specified in config file  override the folders previously added from WebUI. */
,
  "shared_folders" :
  [
    {
      "secret" : "$SECRET", // required field - use --generate-secret in command line to create new secret
      "dir" : "$SYNC_DIR", // * required field
      "use_relay_server" : true, //  use relay server when direct connection fails
      "use_tracker" : true,
      "use_dht" : false,
      "search_lan" : true,
      "use_sync_trash" : true, // enable SyncArchive to store files deleted on remote devices
      "overwrite_changes" : false, // restore modified files to original version, ONLY for Read-Only folders
      "known_hosts" : // specify hosts to attempt connection without additional search
      [
//        "192.168.1.2:44444"
      ] 
    }
  ]



